function getMapCenterPixels() {
    // Get the elements
    const mapDiv = document.querySelector(`.gm-style div div div div div `);
    const mapDivMove = document.querySelector(`.gm-style div div`);

    if(mapDiv !== null){
        // console.log(mapDiv.style)
        console.log(mapDivMove.style)
    }
        


    return ;
}


export default getMapCenterPixels;


